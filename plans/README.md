# Implementation Plans

Generated by the improve skill on 2026-06-11. This index now covers the full vetted finding set from the standard and deep audit passes. Execute in the order below unless dependencies say otherwise. Each executor: read the plan fully before starting, honor its STOP conditions, and update your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Normalize runtime domain assumptions | P1 | M | - | TODO |
| 002 | Push marketplace discovery filtering and lookup hydration server-side | P1 | M | - | TODO |
| 004 | Collapse dispute admin queue N+1 hydration | P1 | M | - | TODO |
| 005 | Normalize notification error handling and add coverage for preferences flows | P1 | M | - | TODO |
| 003 | Eliminate release-only Firebase config drift | P2 | M | - | TODO |
| 006 | Split the monolithic app router into modular route builders | P2 | L | - | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale - finding fixed independently or approach abandoned)

## Dependency notes

- Plan 001 stands alone but should land first because it aligns with the active domain-hardcoding cleanup effort and removes a cross-cutting runtime risk.
- Plan 002 is independent of the config/domain work and can run in parallel if different executors are available.
- Plan 004 is independent of the earlier plans and targets a separate admin-only performance hotspot.
- Plan 005 is independent, but it is easiest to review after Plan 004 because both touch admin/support/notification operational reliability.
- Plan 003 is independent, but should be reviewed by whoever owns release infrastructure because it changes how Firebase config is sourced and validated.
- Plan 006 should land after any in-flight routing feature work settles, because it is a structural refactor over a file that is currently under active development.

## Findings considered and rejected

- Committed Firebase API keys in `lib/firebase_options.dart`: rejected as a security finding because Firebase client config is public-by-design; the real issue is config drift between analyzed code and the release artifact, which Plan 003 covers.
- Missing verification baseline: rejected because `flutter analyze --fatal-infos` and targeted core/auth unit suites passed during recon, so the repo already has a working baseline.
- Broad "add more tests" recommendation: rejected as too generic. Plans 001 and 002 each require targeted characterization tests where the gaps create concrete risk.
- Seller approval lookup fail-closed in `lib/features/auth/providers/auth_route_resolution_provider.dart`: rejected as a correctness bug because current widget tests explicitly codify that fallback behavior when approval lookup fails.
